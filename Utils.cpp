#include "Utils.h"
#include <iostream>
using namespace std;

/*const string& Utils::duplicateString(const string& instr) {
	
	int length = instr.length + 1;
	instr.copy()
	for (int i = 0; i < length; i++)
		outElecFilestr[i] = instr[i];
	return outElecFilestr;
}*/

